============
Installation
============

At the command line::

    $ easy_install django-private-chat

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-private-chat
    $ pip install django-private-chat
